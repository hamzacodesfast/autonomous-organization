{
  email {$ACME_EMAIL}
}

www.{$AO_DOMAIN} {
  redir https://{$AO_DOMAIN}{uri} permanent
}

{$AO_DOMAIN} {
  encode zstd gzip

  header {
    X-Content-Type-Options nosniff
    Referrer-Policy strict-origin-when-cross-origin
    X-Frame-Options DENY
  }

  reverse_proxy 127.0.0.1:3000
}
